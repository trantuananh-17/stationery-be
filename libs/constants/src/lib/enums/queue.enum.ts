export enum QUEUE_GROUPS {
  MAIL = 'mail',
  ORDER = 'order',
  PRODUCT = 'product',
  USER = 'user',
}

export enum QUEUE_SERVICES {
  BFF = 'bff',
  ORDER = 'order',
  PRODUCT = 'product',
  USER = 'user',
  AUTH = 'auth',
}
